/**
 * @module @ands/guidelines
 * @description ANDS guideline registry plugin.
 *
 * Provides the `ands guideline` command with subcommands:
 * list, get <id>, validate, add.
 */

export { guidelinesPlugin } from './plugin.js';
