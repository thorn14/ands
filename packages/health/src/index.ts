/**
 * @module @ands/health
 * @description ANDS design-system health metrics plugin.
 *
 * Provides the `ands audit` command with built-in metrics:
 * token-coverage, a11y-rate, doc-freshness.
 */

export { healthPlugin } from './plugin.js';
